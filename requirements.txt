# Torch first: CPU wheels come from PyPI by default.
# For NVIDIA GPUs see the README (install from the cu126 index instead).
torch>=2.2
torchaudio>=2.2

# web service
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7

# voice cloning engine (F5-TTS v1, zero-shot, MIT-licensed code)
f5-tts>=1.1.0

# reference-audio transcription (auto-generates ref_text)
faster-whisper>=1.0

# speaker-similarity scoring (ECAPA-TDNN embeddings)
speechbrain>=1.0

# audio I/O
soundfile>=0.12.1
librosa>=0.10

# tests / scripts
httpx>=0.27
pytest>=8.0
